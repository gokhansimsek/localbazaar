# Flat (npm-style) node_modules so the layout survives `COPY --from=deps` across
# Docker build stages. Without this, pnpm's symlinks point into a content-addressed
# store that the runtime stage doesn't have.
node-linker=hoisted
